# Datasets are committed to this repo intentionally so the data ships with the code.
# Ignored below are only redundant artefacts whose usable form is already tracked.
data/**/*.zip
data/online-retail/*.xlsx

# Python
__pycache__/
*.py[cod]
.venv/
venv/
